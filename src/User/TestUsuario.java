
// Jaja quien sabe porque se quito mi test de usuario
/*
package User;
import User.*;
import static org.junit.Assert.assertEquals;
import org.junit.Test;

import RegistroCitas.AppointmentBase;

public class TestUsuario {
    private Usuario usuario = new Usuario("Juanito", "Pérez", "juan.perez@example.com", "123456", "222000000", "Calle 123", "Cholula", "Puebla", "12345", "Mexico", new AppointmentBase());

    @Test
    public void testConstructor() {
        assertEquals("Juanito", usuario.getNombre());
        assertEquals("Pérez", usuario.getApellido());
        assertEquals("juan.perez@example.com", usuario.getEmail());
        assertEquals("123456", usuario.getPassword());
        assertEquals("222000000", usuario.getTelefono());
        assertEquals("Calle 123", usuario.getDireccion());
        assertEquals("Cholula", usuario.getCiudad());
        assertEquals("Puebla", usuario.getEstado());
        assertEquals("12345", usuario.getCodigoPostal());
        assertEquals("Mexico", usuario.getPais());
    }

    @Test
    public void testSettersAndGetters() {
        usuario.setNombre("Carlito");
        usuario.setApellido("Gómez");
        usuario.setEmail("carlos.gomez@example.com");
        usuario.setPassword("newpassword");
        usuario.setTelefono("0987654321");
        usuario.setDireccion("Avenida Siempre Viva 742");
        usuario.setCiudad("Ciudad Nueva");
        usuario.setEstado("Estado Nuevo");
        usuario.setCodigoPostal("54321");
        usuario.setPais("País Nuevo");

        assertEquals("Carlito", usuario.getNombre());
        assertEquals("Gómez", usuario.getApellido());
        assertEquals("carlos.gomez@example.com", usuario.getEmail());
        assertEquals("newpassword", usuario.getPassword());
        assertEquals("0987654321", usuario.getTelefono());
        assertEquals("Avenida Siempre Viva 742", usuario.getDireccion());
        assertEquals("Ciudad Nueva", usuario.getCiudad());
        assertEquals("Estado Nuevo", usuario.getEstado());
        assertEquals("54321", usuario.getCodigoPostal());
        assertEquals("País Nuevo", usuario.getPais());
    }

    @Test
    public void testToString() {
        String expected = "Usuario [apellido=Pérez, ciudad=Cholula, codigoPostal=12345, direccion=Calle 123, " +
                          "email=juan.perez@example.com, estado=Puebla, nombre=Juanito, pais=Mexico, " +
                          "password=123456, telefono=222000000]";
        assertEquals(expected, usuario.toString());
    }
} */

// Lo comente porque ya ni instalaron el JUnit
